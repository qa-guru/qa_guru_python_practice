# Часть 7. Инструменты DevTools: как эффективно искать локаторы

DevTools — основной инструмент для анализа DOM, выбора устойчивых локаторов и диагностики поведения интерфейса. Чтобы студент мог эффективно работать с локаторами, важно понимать базовые функции DevTools и уметь различать инструменты начального и продвинутого уровня.

---

## Базовые инструменты DevTools

### 🔹 Открытие DevTools и Inspect Element

**Как открыть DevTools:**
- ПКМ по элементу → **Inspect (Просмотреть код)**
- или сочетания клавиш:
  - **Ctrl + Shift + I** / **Ctrl + Shift + C** — Windows
  - **Cmd + Option + I** / **Cmd + Option + C** — macOS.

**Задачи Inspect Element:**
- выделение элемента на странице
- просмотр HTML-структуры
- анализ атрибутов (`id`, `class`, `name`, `data-*`, `aria-*`)
- определение местоположения элемента в DOM.

---

### Панель Elements: поиск и проверка локатора

Панель **Elements** — основная зона работы с локаторами.

Возможности:
- разворачивание узлов DOM
- отслеживание изменений структуры при взаимодействии с интерфейсом
- изучение итоговой структуры после рендеринга
- проверка CSS и XPath локаторов.

**Проверка локатора:**
1. Открыть DevTools → панель **Elements**.
2. Нажать **Ctrl + F** (Windows) или **Cmd + F** (macOS).
3. Ввести CSS или XPath селектор.
4. Проверить совпадения.

**Оценка результата:**
- хорошо: **1/1** — найден один элемент
- допустимо: несколько совпадений, если планируется уточнение области поиска
- плохо: `0` совпадений или слишком много элементов.

---

### На что обращать внимание при выборе локатора
- наличие устойчивых атрибутов: `data-testid`, `data-qa`, `data-test`
- наличие стабильного `id`
- наличие бизнес-значимых классов (не технических `sc-*`, `css-*`)
- использование `name`, `role`, `aria-*`.

**Общие рекомендации:**
- начинать с CSS-селекторов
- избегать абсолютных путей вида `#root > div > div:nth-child(2)`
- избегать индексов, если есть альтернативные признаки.

---

### Копирование селекторов (как подсказка)
ПКМ по элементу → **Copy** →
- **Copy selector**
- **Copy JS Path**
- **Copy XPath**.

> Эти селекторы нужно использовать только как подсказку.
> Они часто избыточны, хрупки или содержат технические индексы.

После копирования локатор рекомендуется упростить и привести в соответствие с best practices.

---

### Console: ручное тестирование селекторов
Консоль позволяет проверять корректность локаторов.

**CSS:**
```js
document.querySelectorAll('[data-test="save-btn"]')
document.querySelectorAll('.user-item').length
```

**XPath:**
```js
$x('//button[@id="login"]')
```

Проверяем:
- уникальность
- соответствие ожидаемому элементу
- отсутствие ложных совпадений

---

## Продвинутые инструменты DevTools

### Rendering Tools: визуализация динамики интерфейса
Панель **Rendering** помогает анализировать нестабильные части интерфейса.


**Как открыть Rendering Tools:**
1. Открыть DevTools
2. Нажать на три точки ⋮ в правом верхнем углу панели DevTools
3. Выбрать More tools
4. Выбрать Rendering

**Полезные инструменты:**
- **Paint flashing** — подсветка перерисовываемых областей
- **Layout Shift Regions** — отображение смещений элементов

Помогает выявлять:
- элементы, которые "мигают"
- skeleton-блоки
- скрытые или подгружаемые элементы

---

### DOM Breakpoints: работа с исчезающими элементами
Некоторые элементы (лоадеры, спиннеры, skeleton) отображаются очень короткое время.

**Как установить breakpoint:**
1. Панель **Elements**.
2. ПКМ по узлу → **Break on**
3. Выбрать:
   - **Node removal** — остановка при удалении элемента
   - **Subtree modifications** — изменение структуры
   - **Attribute modifications** — изменение атрибутов

При срабатывании breakpoint можно увидеть:
- атрибуты исчезающего элемента
- какое событие вызвало удаление
- как часто элемент появляется/исчезает
- поведение фреймворка (React/Vue/Angular) при ререндере

